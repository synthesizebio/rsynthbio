# Transform Metadata Model Output (Internal)

Default output transformer, does not modify the response from the
server.

## Usage

``` r
default_output_transformer(final_json)
```

## Arguments

- final_json:

  The parsed API response list

## Value

the same final_json
