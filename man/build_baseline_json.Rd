% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/self-hosted.R
\name{build_baseline_json}
\alias{build_baseline_json}
\title{Build a Baseline JSON-Shaped List from an Arrow Table (Internal)}
\usage{
build_baseline_json(table, gene_order)
}
\arguments{
\item{table}{An Arrow `Table`.}

\item{gene_order}{Character vector of gene ids (column order for `counts`).}
}
\value{
A list with `gene_order` and `outputs` (counts, metadata, latents).
}
\description{
Reshapes the per-row Arrow columns (`counts`, `metadata`, and the
three latent list columns) into the same nested list structure that
[transform_baseline_output()] consumes, so the JSON and Arrow paths share one
transformer and return an identical shape.
}
\keyword{internal}
